{
  "private": true,
  "scripts": {
    "build:sass": "sass --no-source-map -s compressed style:_site/img",
    "build:site": "eleventy",
    "build": "run-p build:*",
    "watch:sass": "npm run build:sass -- --watch",
    "watch:site": "npm run build:site -- --serve --incremental",
    "start": "run-p watch:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "markdown-it-anchor": "^5.3.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.10"
  }
}
